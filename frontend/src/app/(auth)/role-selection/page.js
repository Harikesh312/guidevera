import RoleSelectionClient from './RoleSelectionClient';

export const metadata = {
  title: 'Select Your Role – Guidevera',
  description: 'Choose your role on Guidevera to get a personalised experience tailored for students, counselors, or administrators.',
};

export default function RoleSelectionPage() {
  return <RoleSelectionClient />;
}
